{
  "rules": [
    {
      "rulePriority": 1,
      "description": "keep the 15 most recent images (rollback depth), expire older ones",
      "selection": {"tagStatus": "any", "countType": "imageCountMoreThan", "countNumber": 15},
      "action": {"type": "expire"}
    }
  ]
}
